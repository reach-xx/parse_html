from distutils.core import setup

setup(name='Makeup',
      version='1.0',
      description='A html parse',
      author='reach',
      py_modules=['parse', 'handlers.handlers', 'rules.rules', 'util.util'])
# import py2exe
# setup(
#       console=['parse.py']
# )

